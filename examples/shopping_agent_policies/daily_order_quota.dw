@id("shopping_daily_order_quota")
@hint("daily order quota has been reached")
permit (
    principal,
    action == Agent::Action::"CheckoutCart",
    resource
)
when temporal {
    exists (n: Long). (
        (count for (t: Timepoint). where (
            formerly within 1d (
                Agent::Action::"CheckoutCart"::request{
                    input.input.user: context.input.input.user,
                    input.input.status: "completed"
                }
                && tp(t)
            )
        )) == n
        && n < 3
    )
};
